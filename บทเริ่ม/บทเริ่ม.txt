1. ตัวแปร (Variable) คืออะไร?
ตัวแปรคือกล่องที่เก็บข้อมูล เช่น ชื่อ, อายุ, คะแนน ฯลฯ

name = "บาส"
age = 20
score = 95.5

name คือ ตัวแปรเก็บข้อความ "บาส"

age คือ ตัวแปรเก็บตัวเลขจำนวนเต็ม 20

score คือ ตัวแปรเก็บเลขทศนิยม 95.5
======================================
🔹 2. การแสดงผล (print)
ใช้คำสั่ง print() เพื่อแสดงข้อมูลออกมา
📌 ตัวอย่าง:
print(name)
print("อายุของฉันคือ", age)
print("คะแนนสอบคือ", score)
✅ ผลลัพธ์:
บาส
อายุของฉันคือ 20
คะแนนสอบคือ 95.5
===========================================
✅ ต่อไป: บทที่ 2 — การรับข้อมูลจากผู้ใช้ (Input)
เวลาทำโปรแกรม เราไม่อยากให้ทุกอย่าง "เขียนตายตัว" ถูกมั้ยครับ
เราสามารถให้ผู้ใช้ กรอกข้อมูลเอง ได้ ด้วยคำสั่ง input()

name = input("กรอกชื่อของคุณ: ")
age = input("กรุณาใส่อายุ: ")

print("สวัสดี", name)
print("คุณอายุ", age, "ปี")

🧠 จุดสังเกต:

input() จะให้ผู้ใช้พิมพ์ข้อมูลเข้ามา

ข้อมูลที่ได้จาก input() จะเป็น “ข้อความ” เสมอ
(แม้จะพิมพ์เลขก็ยังถือว่าเป็นข้อความ)

💡 ถ้าต้องการใช้ตัวเลข ต้องแปลงก่อน:
age = int(input("อายุของคุณ: ")) 
=============================================
🧠 บทที่ 3: คำสั่งเงื่อนไข if, else, elif
ใช้เพื่อตรวจสอบ "เงื่อนไข" แล้วสั่งให้โปรแกรมทำงานตามที่กำหนด
เช่น ถ้าอายุมากกว่า 18 แสดงว่าเป็นผู้ใหญ่

age = int(input("กรุณากรอกอายุของคุณ: "))

if age >= 18:
    print("คุณเป็นผู้ใหญ่")
else:
    print("คุณยังเป็นเยาวชน")
	
	🧠 คำอธิบาย:

if = ถ้าเงื่อนไขเป็นจริง

else = ถ้าไม่จริง ให้ทำอย่างอื่น

: ต้องใส่เสมอหลัง if และ else

บรรทัดที่อยู่ในเงื่อนไข ต้อง “ย่อหน้า” (indent) เข้าไป

 ตัวอย่าง 2: if / elif / else

score = int(input("กรุณาใส่คะแนนของคุณ: "))

if score >= 80:
    print("เกรด A")
elif score >= 70:
    print("เกรด B")
elif score >= 60:
    print("เกรด C")
else:
    print("ตกนะครับ สู้ใหม่!")
=======================================
🔄 บทที่ 4: การวนซ้ำ (Loop)
การ "วนซ้ำ" คือการสั่งให้โปรแกรมทำอะไรซ้ำๆ โดยไม่ต้องพิมพ์ซ้ำเอง
เช่น พิมพ์คำว่า "Hello" 5 ครั้ง
🔹 มี 2 แบบหลักๆ:
for loop — วนตามจำนวนครั้ง

while loop — วนจนกว่าเงื่อนไขจะหยุด
🔸 for loop แบบง่าย
for i in range(5):
    print("สวัสดี")

🔸 while loop
	i = 0
while i < 5:
    print("รอบที่", i)
    i += 1  # เพิ่มค่า i ทีละ 1
=============================================
🧠 บทที่ 5: ฟังก์ชัน (Function)
ฟังก์ชันคือ “ชุดคำสั่ง” ที่เราตั้งชื่อไว้ แล้วเรียกใช้ได้ตอนไหนก็ได้
เหมือนสูตรอาหารที่ใช้ซ้ำได้หลายครั้ง ไม่ต้องเขียนใหม่ทุกครั้ง

def say_hello():
    print("สวัสดีจ้า!")
    print("ยินดีต้อนรับ")
	
	def = เริ่มต้นประกาศฟังก์ชัน

say_hello() = ชื่อฟังก์ชัน (ตั้งชื่อเองได้)

() = ช่องไว้ใส่ "ข้อมูลที่ส่งเข้าไป" (ถ้ามี)

ต้องย่อหน้าด้านในเสมอ (indent)

 การเรียกใช้ฟังก์ชัน
say_hello()
say_hello()

✅ โค้ดของคุณทำงานแบบนี้:
name = input("กรอกชื่อของคุณ")
age = int(input("กรอกอายุของคุณ"))

def show_info(name, age):
    print("สวัสดี", name)
    print("คุณอายุ", age)

show_info(name, age)

📋 ผลลัพธ์:

กรอกชื่อของคุณ: บาส
กรอกอายุของคุณ: 25
สวัสดี บาส
คุณอายุ 25
==============================================